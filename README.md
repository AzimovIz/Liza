# Liza

This is a Smart Home project
It runs on a home server using a discord bot as its interface.
In conjunction with arduino modules, many processes are automated.

At the moment, the project has not been fully implemented.
